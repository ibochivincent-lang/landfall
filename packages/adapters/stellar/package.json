{
  "name": "@landfall/adapter-stellar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Keystone-chain ChainAdapter: reads Stellar settlement history via Horizon. maxTier: PROVEN.",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
