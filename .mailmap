# Canonical author identity for the project owner.
#
# Kept as a guard rather than a fixup: history now stores a single identity,
# so this only catches a future commit made from a machine whose git config
# has drifted, and folds it back rather than adding a fourth name to the log.

Ibochi Vincent <290086463+ibochivincent-lang@users.noreply.github.com> <ibochivincent@gmail.com>
